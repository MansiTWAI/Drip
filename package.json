{
  "name": "drip-commerce",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev:store": "pnpm --dir frontend dev",
    "dev:admin": "pnpm --dir admin dev",
    "dev:api": "pnpm --dir backend start",
    "build": "npm run build:vercel",
    "build:vercel": "npm --prefix frontend ci --no-audit --no-fund && npm --prefix admin ci --no-audit --no-fund && npm --prefix frontend run build && npm --prefix admin run build && node scripts/assemble-vercel-build.mjs",
    "lint": "pnpm --dir frontend lint && pnpm --dir admin lint"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cloudinary": "^2.9.0",
    "cors": "^2.8.6",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.1.5",
    "multer": "^2.0.2",
    "nodemailer": "^8.0.1",
    "validator": "^13.15.26"
  },
  "engines": {
    "node": ">=20"
  }
}
